FloatLayout:

    ScreenManager:
        id: screen_manager

        LoginScreen:
        RegistrationScreen:
        PasswordForgotScreen:
        PasswordVerifyScreen:
        PasswordResetScreen:
        PortalScreen:

    AnchorLayout:
        anchor_x: "center"
        anchor_y: "bottom"

        FMNotification:
            id: notifier
            auto_dismiss: True
